import {merge} from './merge.js'
import * as kvFormat from './kvFormat'
import * as common from './common'
import {complieVarString, splice, compile} from './url'

export default {
    common,
    merge,
    complieVarString,
    splice,
    compile,
    kvFormat
}
